on:
  push:
    branches:
    - master

name: Produce dist/index.js

jobs:
  build:
    name: npm run build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Build project
        run: |
          npm install && npm run build
